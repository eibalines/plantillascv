<div class="dropbox">
          <input type="file" multiple  @change="recogerImg($event.target.name, $event.target.files)"
            accept="image/*" class="input-file">
         </div>



recogerImg(name, files) {
    this. nombre = name;
    console.log(name);
    
  //this will return an ARRAY of File object
    console.log(files);

  return (

    <input type="file" onChange={this.recogerDatos}/>
    )
},